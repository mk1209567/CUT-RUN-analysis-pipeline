to generate summary report
1. filter_bamfile.sh  :  use filter_loop.sh to filter bam files first (remove chrM and keep mapping_quality >= 30)
2. generate_summary.sh  : this will generate summary for fastq, bowtie alignment, and picard mark duplication. And combine them into a single report.